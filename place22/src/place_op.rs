pub struct PlaceOp {
    pub toff: u32,
    pub censor: bool,
    pub r0: u16,
    pub c0: u16,
    pub r1: u16,
    pub c1: u16,
    pub palette_i: u8,
    pub old_palette_i: u8,
    pub uint_id: u32
}
